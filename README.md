
  # Premium AI Healthcare Website

  This is a code bundle for Premium AI Healthcare Website. 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
